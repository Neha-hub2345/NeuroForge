FROM jenkins/jenkins:lts

# Switch to root to install packages and access docker.sock
USER root

# Install Docker CLI and clean up apt cache to keep image size small
RUN apt-get update && \
    apt-get install -y docker.io && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*